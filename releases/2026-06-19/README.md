
## Notes

This release is the first for the GOEx pipeline; reducing to 171 species but increasing the overall number of annotations by 2x by including more (and more homogeneous) data for each species.

## Errata

While all data files are correct, the products/solr build used a cached ontology; a secondary refresh was run for the solr build and that was put into production. 
