To validate locally, use:

```
python3 -m venv venv
source venv/bin/activate
pip install linkml
linkml-validate -s metadata/goex.linkmlschema.yaml metadata/goex.yaml
linkml-convert -t json -s metadata/goex.linkmlschema.yaml metadata/goex.yaml
```
